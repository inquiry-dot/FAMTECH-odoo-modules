<odoo>
  <data noupdate="1">
    <record id="email_template_custom_invoice" model="mail.template">
      <field name="name">Custom Invoice</field>
      <field name="model_id" ref="account.model_account_move"/>
      <field name="subject">Invoice ${object.name}</field>
      <field name="email_from">${(user.email_formatted or '')|safe}</field>
      <field name="email_to">${object.partner_id.email}</field>
      <field name="body_html"><![CDATA[
        <p>Dear ${object.partner_id.name},</p>

        <p>Please find attached your invoice <strong>${object.name}</strong>.</p>

        <p>Total Amount: <strong>${format_amount(object.amount_total, object.currency_id)}</strong></p>

        <p>Due Date: <strong>${object.invoice_date_due}</strong></p>

        <p>Thank you for your business.</p>
      ]]></field>

      <field name="report_template" ref="famtech_customizations.report_invoice_custom"/>
      <field name="report_name">Invoice_${object.name}</field>
    </record>
  </data>
</odoo>